# Data Source created from Pipe 'mv_average_tabcount_15mins'

SCHEMA >
    `intervalStart` DateTime,
    `userEmail` String,
    `instanceId` String,
    `averageCount` AggregateFunction(avg, Int16)

ENGINE "AggregatingMergeTree"
ENGINE_PARTITION_KEY "toYYYYMM(intervalStart)"
ENGINE_SORTING_KEY "intervalStart, userEmail, instanceId"
