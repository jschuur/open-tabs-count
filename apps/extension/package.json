{
  "name": "extension",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "dev": "node esbuild.js --watch",
    "build": "pnpm run clean && node esbuild.js",
    "build:prod": "pnpm run clean && NODE_ENV=production node esbuild.js",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "tb:pull:force": "tb pull --folder tinybird --force"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.14.10",
    "@types/pluralize": "^0.0.33",
    "@types/uuid": "^10.0.0",
    "dotenv-mono": "^1.3.14",
    "esbuild": "^0.23.0",
    "glob": "^11.0.0",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "@chronark/zod-bird": "^0.3.9",
    "picocolors": "^1.0.1",
    "pluralize": "^8.0.0",
    "uuid": "^10.0.0",
    "zod": "^3.23.8"
  }
}
